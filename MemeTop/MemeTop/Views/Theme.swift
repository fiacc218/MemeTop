import SwiftUI

enum Theme {
    static let up = Color.green
    static let down = Color.red
    static let upBg = Color.green.opacity(0.12)
    static let downBg = Color.red.opacity(0.12)
    static let floatUp = Color.green
    static let floatDown = Color.red
}
